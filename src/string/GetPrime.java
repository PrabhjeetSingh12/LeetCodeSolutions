package string;

import java.util.ArrayList;
import java.util.List;

public class GetPrime {
    public List<Integer> getPrime(int[] numbers) {
        List<Integer> primes = new ArrayList<>();

        for (int i = 0; i < numbers.length; i++) {
            int count = 0;
        }
        return new ArrayList<>();
    }

}
